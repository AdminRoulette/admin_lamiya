async function kastaPurposes(purposes, paramArray) {
    if (!purposes) {
        return false;
    }
    let purposesArray = [];
    for (const purpose of purposesArray) {
        if (purpose.id === 983) {
            purposesArray.push('Еластичність');
        }
        if (purpose.id === 80) {
            purposesArray.push('Від січення волосся');
        }
        if (purpose.id === 95) {
            purposesArray.push('Антивіковий');
        }
        if (purpose.id === 68) {
            purposesArray.push('Антибактеріальний');
        }
        if (purpose.id === 90) {
            purposesArray.push('Антицелюліт');
        }
        if (purpose.id === 50) {
            purposesArray.push('Відновлення');
        }
        if (purpose.id === 56) {
            purposesArray.push('Для стимуляції росту');
        }
        if (purpose.id === 983) {
            purposesArray.push('Для пружності');
        }
        if (purpose.id === 65) {
            purposesArray.push('Матуючий');
        }
        if (purpose.id === 66) {
            purposesArray.push('Освіження');
        }
        if (purpose.id === 69) {
            purposesArray.push('Відлущування');
        }
        if (purpose.id === 85) {
            purposesArray.push('Охолодження');
        }
        if (purpose.id === 92) {
            purposesArray.push('Очищення');
        }
        if (purpose.id === 57) {
            purposesArray.push('Живлення');
        }
        if (purpose.id === 72) {
            purposesArray.push('Протизапальний');
        }
        if (purpose.id === 86) {
            purposesArray.push('Розгладження');
        }
        if (purpose.id === 94) {
            purposesArray.push("Пом'якшення");
        }
        if (purpose.id === 61) {
            purposesArray.push('Зволоження');
        }
        if (purpose.id === 63) {
            purposesArray.push('Зміцнення');
        }
        if (purpose.id === 58) {
            purposesArray.push('Заспокоєння');
        }
        if (purpose.id === 1106) {
            purposesArray.push('Фіксація');
        }
        if (purpose.id === 91) {
            purposesArray.push('Освітлення');
        }
        if (purpose.id === 55) {
            purposesArray.push('Для масажу');
        }
        if (purpose.id === 76) {
            purposesArray.push('Тонізування');
        }
        if (purpose.id === 49) {
            purposesArray.push('Відбілювання');
        }
        if (purpose.id === 96) {
            purposesArray.push('Для об’єму');
        }
        if (purpose.id === 77) {
            purposesArray.push('Тонуюче');
        }
        if (purpose.id === 53) {
            purposesArray.push('Для блиску');
        }
        if (purpose.id === 79) {
            purposesArray.push('Від лупи');
        }
        if (purpose.id === 88) {
            purposesArray.push('Проти випадіння волосся');
        }
        if (purpose.id === 82) {
            purposesArray.push('Протинабрякове');
        }
        if (purpose.id === 982) {
            purposesArray.push('Від розтяжок');
        }
        if (purpose.id === 983) {
            purposesArray.push('Підтягуюче');
        }
        if (purpose.id === 89) {
            purposesArray.push('Від зморшок');
        }
        if (purpose.id === 83) {
            purposesArray.push('Регенерація');
        }
        if (purpose.id === 59) {
            purposesArray.push('Захист від сонця');
        }
        if (purpose.id === 985) {
            purposesArray.push('Омолодження');
        }
        if (purpose.id === 44) {
            purposesArray.push('Від перших ознак старіння');
        }
        if (purpose.id === 71) {
            purposesArray.push('Лікування акне');
        }
        if (purpose.id === 62) {
            purposesArray.push('Звуження пір');
        }
        if (purpose.id === 73) {
            purposesArray.push('От пигментации');
        }
        if (purpose.id === 67) {
            purposesArray.push('Антиоксидантну дію');
        }
        if (purpose.id === 46) {
            purposesArray.push('Від почервоніння');
        }
        if (purpose.id === 64) {
            purposesArray.push('Ліфтинг');
        }
        if (purpose.id === 82) {
            purposesArray.push('Від набряків');
        }
        if (purpose.id === 75) {
            purposesArray.push('Себорегуліровання');
        }
        if (purpose.id === 60) {
            purposesArray.push('Захист кольору');
        }
        if (purpose.id === 56) {
            purposesArray.push('Для росту');
        }
        if (purpose.id === 54) {
            purposesArray.push('Для засмаги');
        }
        if (purpose.id === 81) {
            purposesArray.push('Для розчісування');
        }
        if (purpose.id === 48) {
            purposesArray.push('Від чорних цяток');
        }
        if (purpose.id === 45) {
            purposesArray.push('Від купероза');
        }
        if (purpose.id === 1088) {
            purposesArray.push('Для кінчиків волосся');
        }
        if (purpose.id === 88) {
            purposesArray.push('Від випадіння');
        }
        if (purpose.id === 87) {
            purposesArray.push('Від подразнення');
        }
        if (purpose.id === 1082) {
            purposesArray.push('Формування локонів');
        }
        if (purpose.id === 1086) {
            purposesArray.push('Моделювання');
        }
        if (purpose.id === 56) {
            purposesArray.push('Посилення росту');
        }
        if (purpose.id === 52) {
            purposesArray.push('Для автозасмаги');
        }
        if (purpose.id === 47) {
            purposesArray.push('Від темних кіл');
        }
        if (purpose.id === 78) {
            purposesArray.push('Випрямлення');
        }
        if (purpose.id === 77) {
            purposesArray.push('Тонування');
        }
        if (purpose.id === 72) {
            purposesArray.push('Від запалень');
        }
        if (purpose.id === 65) {
            purposesArray.push('Матування');
        }
        if (purpose.id === 1085) {
            purposesArray.push('Детокс');
        }
        if (purpose.id === 53) {
            purposesArray.push('Сяйво');
        }
        if (purpose.id === 988) {
            purposesArray.push('Вирівнювання тону');
        }
    }

    if (purposesArray.length > 0) {
        paramArray.push({
            '#': purposesArray.join(","),
            '@name': 'Призначення і результат'
        })
    }
    return true;
}

module.exports = kastaPurposes;
